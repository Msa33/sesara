import Commander from "../components/Commande";
import MainLayout from "../layouts/MainLayout";

const ValiderCommande = () => {
  return (
    <MainLayout>
      <Commander />
    </MainLayout>
  );
};

export default ValiderCommande;
